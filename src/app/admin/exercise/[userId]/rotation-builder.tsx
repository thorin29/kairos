// The rotation editor now lives in each person's own workout card (they build
// their own plan). Re-exported here so any stray import stays valid.
export { RotationBuilder } from "@/app/exercise/rotation-builder";
